import { CheckCheck } from 'lucide-react';

export const Logo = () => (
  <div className="flex items-center gap-2">
    <CheckCheck size={32} strokeWidth={3} />
    <h1 className="text-xl font-bold">The Todo</h1>
  </div>
);
